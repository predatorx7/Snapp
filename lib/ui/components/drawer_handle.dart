import 'package:flutter/material.dart';

Widget drawerHandle = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Container(
    height: 4,
    width: 35,
    decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(10), color: Colors.grey[400]),
  ),
);
